const {mockValues} = require('../test/helpers/constants');
const ChildChainManager = artifacts.require("ChildChainManager");

module.exports = async function (deployer) {
    // const instance = await ChildChainManager.at(mockValues.childChainManager);
    // const initialize = await instance.initialize(mockValues.consensusRate,
    //     mockValues.minValidators,
    //     mockValues.validators,
    //     mockValues.childChainId,
    //     mockValues.accounts[0])
    // console.log('ChildChainManager initialize: ', initialize);
    //
    //
    // const mapToken = await instance.mapToken(mockValues.childTokenErc20,
    //     mockValues.rootChainId,
    //     mockValues.rootTokenErc20);
    // console.log('ChildChainManager mapToken: ', mapToken);
};